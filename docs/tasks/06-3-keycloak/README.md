# Keycloak Integration

**Цель:** Полная интеграция с Keycloak для SSO, управления группами и авторизации
**Статус:** 🔄 В процессе (~65% готово)

---

## Текущее состояние

### Реализовано

| Компонент | Файл | Статус |
|-----------|------|--------|
| OAuth2 Client | `internal/infrastructure/keycloak/oauth_client.go` | ✅ Готово |
| Token Store (Redis) | `internal/infrastructure/auth/token_store.go` | ✅ Готово |
| AuthService | `internal/service/auth_service.go` | ✅ Готово |
| Auth Handler | `internal/handler/http/auth_handler.go` | ✅ Готово |
| Docker Compose | `docker-compose.yml` (Keycloak v23) | ✅ Готово |
| NoOp Keycloak Client | `internal/application/workspace/noop_keycloak_client.go` | ✅ Готово |

### Не реализовано

| Компонент | Описание | Приоритет |
|-----------|----------|-----------|
| Realm Setup | Настройка realm, client, roles в Keycloak | 🔴 Critical |
| JWT Validation | Офлайн валидация токенов через JWKS | 🔴 Critical |
| Group Management | Реальный клиент Admin API для групп | 🟡 High |
| Token Middleware | Middleware для Bearer token в заголовках | 🔴 Critical |
| User Sync | Периодическая синхронизация пользователей | 🟢 Medium |
| Integration Tests | E2E тесты с реальным Keycloak | 🟡 High |

---

## Структура задач

| № | Задача | Файл | Приоритет | Статус |
|---|--------|------|-----------|--------|
| 01 | Keycloak Realm Setup | [01-realm-setup.md](01-realm-setup.md) | 🔴 Critical | ⏳ |
| 02 | JWT Validation | [02-jwt-validation.md](02-jwt-validation.md) | 🔴 Critical | ⏳ |
| 03 | Token Middleware | [03-token-middleware.md](03-token-middleware.md) | 🔴 Critical | ⏳ |
| 04 | Group Management | [04-group-management.md](04-group-management.md) | 🟡 High | ⏳ |
| 05 | User Sync | [05-user-sync.md](05-user-sync.md) | 🟢 Medium | ⏳ |
| 06 | Integration Tests | [06-integration-tests.md](06-integration-tests.md) | 🟡 High | ⏳ |

---

## Зависимости между задачами

```
[01 Realm Setup]
       │
       ├──> [02 JWT Validation]
       │           │
       │           v
       └──> [03 Token Middleware]
                   │
       ┌───────────┴───────────┐
       v                       v
[04 Group Management]   [05 User Sync]
       │                       │
       └───────────┬───────────┘
                   v
         [06 Integration Tests]
```

---

## Архитектура

### Текущая архитектура

```
┌─────────────────────────────────────────────────────────┐
│                    HTTP Requests                         │
└──────────────────────┬──────────────────────────────────┘
                       │
                       v
        ┌──────────────────────────────┐
        │    AuthHandler (HTTP)        │
        │  /auth/login, /auth/logout   │
        └──────────────┬───────────────┘
                       │
                       v
         ┌─────────────────────────────┐
         │     AuthService             │
         │  Login/Logout/Refresh       │
         └──────────┬──────────────────┘
                    │
         ┌──────────┴──────────┬────────────────────────┐
         │                     │                        │
         v                     v                        v
    ┌─────────────────┐ ┌──────────────┐    ┌──────────────────┐
    │ OAuthClient     │ │ TokenStore   │    │ UserRepository   │
    │ (Keycloak HTTP) │ │ (Redis)      │    │ (MongoDB)        │
    └─────────────────┘ └──────────────┘    └──────────────────┘
```

### Целевая архитектура

```
┌─────────────────────────────────────────────────────────┐
│                    HTTP Requests                         │
│              (Authorization: Bearer <token>)             │
└──────────────────────┬──────────────────────────────────┘
                       │
                       v
        ┌──────────────────────────────┐
        │    Token Middleware  [NEW]   │ ◄── JWT Validator [NEW]
        │  Extract & Validate Bearer   │         │
        └──────────────┬───────────────┘         │
                       │                         v
                       │              ┌──────────────────┐
                       │              │ JWKS Cache [NEW] │
                       │              └──────────────────┘
                       v
        ┌──────────────────────────────┐
        │    AuthHandler / Protected   │
        │    Handlers                  │
        └──────────────┬───────────────┘
                       │
         ┌─────────────┴─────────────────┐
         │                               │
         v                               v
┌─────────────────────┐    ┌─────────────────────────┐
│   AuthService       │    │ GroupClient [NEW]       │
│   (existing)        │    │ Keycloak Admin API      │
└─────────────────────┘    └─────────────────────────┘
```

---

## Конфигурация

### Текущая (`configs/config.yaml`)

```yaml
keycloak:
  url: "http://localhost:8090"
  realm: "flowra"
  client_id: "flowra-backend"
  client_secret: "your-client-secret"
  admin_username: "admin"
  admin_password: "admin123"
```

### Дополнительные параметры (после реализации)

```yaml
keycloak:
  url: "http://localhost:8090"
  realm: "flowra"
  client_id: "flowra-backend"
  client_secret: "${KEYCLOAK_CLIENT_SECRET}"
  admin_username: "admin"
  admin_password: "${KEYCLOAK_ADMIN_PASSWORD}"
  # New fields
  jwks_cache_ttl: "1h"        # Кэширование JWKS ключей
  token_leeway: "30s"         # Допуск при проверке exp/iat
  sync_interval: "15m"        # Интервал синхронизации пользователей
```

---

## Docker Compose

Keycloak уже настроен в `docker-compose.yml`:

```yaml
keycloak:
  image: quay.io/keycloak/keycloak:23.0
  ports:
    - "8090:8080"
  environment:
    - KEYCLOAK_ADMIN=admin
    - KEYCLOAK_ADMIN_PASSWORD=admin123
    - KC_DB=dev-file
  command: start-dev
```

---

## Тестирование

### Существующие тесты

- `oauth_client_test.go` — 13 тестов (mock HTTP server)
- `token_store_test.go` — 8 тестов (Redis)
- `auth_service_test.go` — 14 тестов (mocks)
- `noop_keycloak_client_test.go` — интеграционный тест

### Планируемые тесты

- Integration tests с testcontainers (Keycloak)
- E2E auth flow тесты
- Group management тесты
- JWT validation тесты

---

## Ресурсы

### Документация Keycloak
- [Keycloak Admin REST API](https://www.keycloak.org/docs-api/23.0.0/rest-api/)
- [OIDC Endpoints](https://www.keycloak.org/docs/latest/server_admin/#openid-connect-1)
- [Go JWKS Libraries](https://github.com/MicahParks/keyfunc)

### Внутренние документы
- [Phase 1.3.1 Keycloak Integration](../../roadmap/phase-1/task-1.3.1-keycloak-integration.md)
- [Auth Service](../06-2-Services/05-auth-service.md)

---

*Обновлено: 2026-01-06*
